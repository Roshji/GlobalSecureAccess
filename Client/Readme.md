# Global Secure Access Installation Scripts

These installation scripts are useful to install the GSA Client on Windows 11 machines. The script installs the client, but also sets registry keys to optimizes it's performance and hardens its security. 
